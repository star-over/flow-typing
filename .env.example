# Convex cloud dev deployment.
# Run `npx convex dev` to populate .env.local with actual values.
# Format: dev:<deployment-name> for dev tier; URLs follow <deployment-name>.<region>.convex.{cloud,site}.
CONVEX_DEPLOYMENT=
PUBLIC_CONVEX_URL=
PUBLIC_CONVEX_SITE_URL=

# Dev-вход для ИИ-агентов и E2E (ADR 0012). Только локально. Эти PUBLIC_*-флаги
# запекаются в статический build/_app/env.js на этапе vite build из наличного
# .env.local: сборка без них в окружении (CI/prod) → кнопки нет; локальный make
# build с dev-.env.local их запечёт, но build/ gitignored и по сети не отдаётся.
# Серверная половина включается `npx convex env set AUTH_DEV_LOGIN_ENABLED true`
# (dev-deployment). Плюс fail-closed prod-гейт (ADR 0023): dev-deployment должен
# нести `npx convex env set DEPLOY_ENV development`, иначе dev-вход и dev-мутации
# (resetMyProfile / setMyLadderStep) на нём считаются prod и выключены.
# PUBLIC_DEV_LOGIN=true
# PUBLIC_DEV_LOGIN_EMAIL=agent@flow.test
# PUBLIC_DEV_LOGIN_PASSWORD=<минимум 8 символов>
