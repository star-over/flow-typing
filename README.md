# FlowTyping: Приложение для тренировки слепой печати

Это приложение на **Next.js** для тренировки слепой печати, разработанное с использованием **TypeScript, React** и **Tailwind CSS**.

## 1. Философия проекта

**FlowTyping** — это не просто очередной клавиатурный тренажер. В основе проекта лежит философия **"рефлексивно-адаптивного обучения"**. Мы отказываемся от скучного заучивания расположения клавиш в пользу формирования устойчивой мышечной памяти и рефлексов.

Ключевая инновация проекта — **"визуализация движения"**, уникальный интерфейс, который учит руки двигаться правильно, а не просто показывает, куда нажать.

> Подробное описание философии, видения и ключевых принципов проекта вы найдете в директории [`docs/`](./docs/).

## 2. Технологический стек

*   **Фреймворк:** [Next.js](https://nextjs.org/) (с App Router)
*   **Язык:** [TypeScript](https://www.typescriptlang.org/)
*   **Библиотека UI:** [React](https://react.dev/)
*   **Управление состоянием:** [XState](https://xstate.js.org/) (для основной логики), [Zustand](https://zustand-demo.pmnd.rs/) (для настроек UI)
*   **Стилизация:** [Tailwind CSS](https://tailwindcss.com/)
*   **UI Компоненты:** [shadcn/ui](https://ui.shadcn.com/)
*   **Тестирование:** [Vitest](https://vitest.dev/)
*   **Разработка компонентов:** [Storybook](https://storybook.js.org/)
*   **Линтинг:** [ESLint](https://eslint.org/)
*   **База данных (клиент):** `localStorage` и `@tanstack/react-db` для MVP.

## 3. Начало работы

### 3.1. Предварительные требования

Убедитесь, что у вас установлен [Node.js](https://nodejs.org/) (рекомендуется версия 20.x или выше) и [Make](https://www.gnu.org/software/make/).

### 3.2. Установка

1.  Клонируйте репозиторий.
2.  Установите зависимости:
    ```bash
    npm install
    ```

### 3.3. Основные команды (`Makefile`)

Все команды для разработки, сборки и тестирования централизованы в `Makefile`.

*   **Запустить сервер для разработки:**
    ```bash
    make dev
    ```
    Приложение будет доступно по адресу `http://localhost:3000`.

*   **Запустить Storybook для разработки компонентов:**
    ```bash
    make storybook
    ```
    Storybook будет доступен по адресу `http://localhost:6006`.

*   **Запустить тесты:**
    ```bash
    make test
    ```

*   **Создать сборку для продакшена:**
    ```bash
    make build
    ```

*   **Выполнить все проверки (lint, типы, тесты, сборка):**
    > **Внимание!** Эту команду необходимо запускать после внесения любых изменений в код для проверки его целостности.
    ```bash
    make check-all
    ```

## 4. Архитектура и документация

Вся архитектурная документация проекта вынесена в директорию [`docs/`](./docs/). Она служит единой базой знаний и описывает ключевые концепции и соглашения, принятые в проекте.

### Содержание документации:

*   [`01-philosophy-and-vision.md`](./docs/01-philosophy-and-vision.md): Философия, цели и долгосрочное видение проекта.
*   [`02-naming-conventions.md`](./docs/02-naming-conventions.md): Правила именования типов в TypeScript.
*   [`03-ui-viewmodel-contract.md`](./docs/03-ui-viewmodel-contract.md): Спецификация `ViewModel`, связывающей логику и UI.
*   [`04-settings-management-system.md`](./docs/04-settings-management-system.md): Архитектура системы управления настройками.
*   [`05-adaptive-learning-system.md`](./docs/05-adaptive-learning-system.md): Описание адаптивного движка "Dynamic Flow" и используемых метрик.

## 5. Вклад в проект

Для поддержания порядка рекомендуется придерживаться следующих практик:

*   **Ветвление:** Используйте отдельные ветки для каждой новой фичи или исправления (`feat/...`, `fix/...`).
*   **Сообщения коммитов:** Следуйте конвенции [Conventional Commits](https://www.conventionalcommits.org/) для ясности истории изменений.
*   **Локальные проверки:** Перед созданием коммита всегда запускайте `make check-all` для обеспечения качества кода.